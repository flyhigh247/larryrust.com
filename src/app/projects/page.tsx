import { EntryCard, PageIntro } from '@/components/ui';
import { projects } from '@/content/entries';
import { pageMetadata } from '@/lib/metadata';
export const metadata = pageMetadata('Projects', 'Planned and documented application-security projects, with space for reproducible evidence, source code, and case studies.', '/projects/');
export default function Projects() { return <div className="container"><PageIntro eyebrow="PROJECTS" title="The work, with the evidence."><p>A growing collection of hands-on security projects. Each case study will connect the problem, the investigation, and the results.</p></PageIntro><section className="section-bottom" aria-labelledby="project-list"><div className="listing-heading"><h2 id="project-list">Project roadmap</h2><span className="muted-label">No completed projects published yet</span></div><div className="card-grid">{projects.map(entry => <EntryCard key={entry.slug} entry={entry} />)}</div><aside className="editorial-note"><span className="eyebrow">WHAT COMES NEXT</span><p>As projects progress, these outlines will become case studies with architecture, testing methodology, findings, remediation, and linked GitHub evidence.</p></aside></section></div>; }
