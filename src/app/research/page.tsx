import { EntryCard, PageIntro } from '@/components/ui';
import { research } from '@/content/entries';
import { pageMetadata } from '@/lib/metadata';
export const metadata = pageMetadata('Research', 'Future threat models, application-security writeups, experiments, and AI-security research from Larry Rust.', '/research/');
export default function Research() { return <div className="container"><PageIntro eyebrow="RESEARCH & NOTES" title="Questions worth investigating."><p>Threat models, AppSec writeups, experiments, and AI-security research. A place to document the reasoning as carefully as the result.</p></PageIntro><section className="section-bottom" aria-labelledby="research-list"><div className="listing-heading"><h2 id="research-list">On the research agenda</h2><span className="muted-label">Outlines only · No findings published yet</span></div><div className="card-grid research-grid">{research.map(entry => <EntryCard key={entry.slug} entry={entry} kind="research" />)}</div><aside className="editorial-note"><span className="eyebrow">A NOTE ON EVIDENCE</span><p>Future entries will distinguish observed results from assumptions and include enough context to understand their limits. These initial outlines describe topics to explore.</p></aside></section></div>; }
